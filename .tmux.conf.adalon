source ~/.dotfiles/.tmux.conf
# Show battery status in the right corner.
set -g status-right "Battery: #(apm -l)%% #(apm -m) min. [%H:%M] [%d.%m.%Y]"
set -g status-right-length 70
set -g status-interval 15
