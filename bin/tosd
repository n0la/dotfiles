#!/bin/sh

MOUNTDIR=/mnt/sd

PWD=$(pwd)
FOLDER=${MOUNTDIR}/$(basename "${PWD}")

mkdir -p "${FOLDER}" || true

for i in "$@"; do
  rsync -rpi --progress "$i" "${FOLDER}"
done

df -h | grep "${MOUNTDIR}"
